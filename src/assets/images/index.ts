// Logo assets for CineMatch Mobile App
// Note: SVG logos are now rendered directly using react-native-svg in the Logo component
export const LOGO_ASSETS = {
  // Main CineMatch logo (SVG rendered via react-native-svg)
  main: 'logo.svg',
  
  // Alternative logo variant (SVG rendered via react-native-svg)
  alt: 'logo-alt.svg',
};

// Logo dimensions for reference
export const LOGO_DIMENSIONS = {
  main: { width: 551.08, height: 148 },
  alt: { width: 800, height: 200 },
};

// Predefined sizes for consistent usage
export const LOGO_SIZES = {
  small: { width: 80, height: 22 },
  medium: { width: 120, height: 32 },
  large: { width: 180, height: 48 },
  xlarge: { width: 240, height: 64 },
};

export default LOGO_ASSETS; 